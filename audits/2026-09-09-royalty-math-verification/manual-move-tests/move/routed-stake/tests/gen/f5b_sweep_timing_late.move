// GENERATED by royalty-sim movegen. Do not hand-edit.
#[test_only, allow(unused_use)]
module routed_stake::royalty_sim_gen_f5b_sweep_timing_late;

use std::unit_test::{assert_eq, destroy};

const ALICE: address = @0xA1;

public struct GenShare() has drop;
public struct GenCurrency() has drop;
public struct GenParent has key { id: UID }

#[test]
fun f5b_sweep_timing_late() {
    let mut sc = sui::test_scenario::begin(ALICE);
    let mut ep0 = object::new(sc.ctx());
    let gp0 = royalty_pool::pool::new<GenShare, GenCurrency>(&mut ep0);
    let pool0_id = object::id(&gp0);
    gp0.share();
    destroy(ep0);
    sc.next_tx(ALICE);
    let mut rp0 = GenParent { id: object::new(sc.ctx()) };
    let rp0_id = object::id(&rp0);
    let g1 = royalty_pool::pool::new<GenShare, GenCurrency>(&mut rp0.id);
    let pool1_id = object::id(&g1);
    g1.share();
    let r0 = routed_stake::routed_stake::new<GenShare, GenShare>(&mut rp0.id, sui::balance::create_for_testing<GenShare>(1), sc.ctx());
    let routed0_id = object::id(&r0);
    routed_stake::routed_stake::share(r0);
    transfer::share_object(rp0);
    sc.next_tx(ALICE);
    let mut r = sc.take_shared_by_id<routed_stake::routed_stake::RoutedStake<GenShare, GenShare>>(routed0_id);
    let mut parent = sc.take_shared_by_id<GenParent>(rp0_id);
    let mut sp = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    r.register(&mut parent.id, &mut sp);
    sui::test_scenario::return_shared(r);
    sui::test_scenario::return_shared(parent);
    sui::test_scenario::return_shared(sp);
    sc.next_tx(ALICE);
    let mut s2 = royalty_pool::stake::new(sui::balance::create_for_testing<GenShare>(1), sc.ctx());
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool1_id);
    p.register_stake(&mut s2);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let mut s3 = royalty_pool::stake::new(sui::balance::create_for_testing<GenShare>(9), sc.ctx());
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool1_id);
    p.register_stake(&mut s3);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    p.deposit(sui::balance::create_for_testing<GenCurrency>(500));
    assert_eq!(p.balance().value(), 500);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    p.deposit(sui::balance::create_for_testing<GenCurrency>(500));
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    p.deposit(sui::balance::create_for_testing<GenCurrency>(361));
    assert_eq!(p.balance().value(), 1361);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let mut s4 = royalty_pool::stake::new(sui::balance::create_for_testing<GenShare>(20), sc.ctx());
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool1_id);
    p.register_stake(&mut s4);
    assert_eq!(p.balance().value(), 0);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let mut r = sc.take_shared_by_id<routed_stake::routed_stake::RoutedStake<GenShare, GenShare>>(routed0_id);
    let mut sp = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    let mut rp = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool1_id);
    r.sweep(&mut sp, &mut rp, rp0_id);
    sui::test_scenario::return_shared(r);
    sui::test_scenario::return_shared(sp);
    sui::test_scenario::return_shared(rp);
    sc.next_tx(ALICE);
    let p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool1_id);
    assert_eq!(p.pending_rewards(&s2), 45);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool1_id);
    assert_eq!(p.pending_rewards(&s3), 408);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool1_id);
    assert_eq!(p.pending_rewards(&s4), 907);
    sui::test_scenario::return_shared(p);
    destroy(s2);
    destroy(s3);
    destroy(s4);
    sc.end();
}

