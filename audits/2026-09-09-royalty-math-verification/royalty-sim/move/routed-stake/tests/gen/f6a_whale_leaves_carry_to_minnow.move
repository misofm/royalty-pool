// GENERATED by royalty-sim movegen. Do not hand-edit.
#[test_only, allow(unused_use)]
module routed_stake::royalty_sim_gen_f6a_whale_leaves_carry_to_minnow;

use std::unit_test::{assert_eq, destroy};

const ALICE: address = @0xA1;

public struct GenShare() has drop;
public struct GenCurrency() has drop;
public struct GenParent has key { id: UID }

#[test]
fun f6a_whale_leaves_carry_to_minnow() {
    let mut sc = sui::test_scenario::begin(ALICE);
    let mut ep0 = object::new(sc.ctx());
    let gp0 = royalty_pool::pool::new<GenShare, GenCurrency>(&mut ep0);
    let pool0_id = object::id(&gp0);
    gp0.share();
    destroy(ep0);
    let mut s0 = royalty_pool::stake::new(sui::balance::create_for_testing<GenShare>(18446744073709551614), sc.ctx());
    let mut s1 = royalty_pool::stake::new(sui::balance::create_for_testing<GenShare>(1), sc.ctx());
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    p.register_stake(&mut s0);
    assert_eq!(p.balance().value(), 0);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    p.register_stake(&mut s1);
    assert_eq!(p.balance().value(), 0);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    p.deposit(sui::balance::create_for_testing<GenCurrency>(18));
    assert_eq!(p.balance().value(), 18);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    assert_eq!(p.pending_rewards(&s0), 0);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    assert_eq!(p.pending_rewards(&s1), 0);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    let reward = p.claim_rewards(&mut s0);
    assert_eq!(p.balance().value(), 18);
    sui::balance::destroy_for_testing(reward);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    p.unregister_stake(&mut s0);
    assert_eq!(p.balance().value(), 18);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    p.deposit(sui::balance::create_for_testing<GenCurrency>(1));
    assert_eq!(p.balance().value(), 19);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    assert_eq!(p.pending_rewards(&s1), 19);
    sui::test_scenario::return_shared(p);
    sc.next_tx(ALICE);
    let mut p = sc.take_shared_by_id<royalty_pool::pool::RoyaltyPool<GenShare, GenCurrency>>(pool0_id);
    let reward = p.claim_rewards(&mut s1);
    assert_eq!(reward.value(), 19);
    assert_eq!(p.balance().value(), 0);
    assert_eq!(p.staked_shares(), 1);
    assert_eq!(p.cumulative_reward_per_share(), 19000000000000000000u256);
    assert_eq!(p.carry(), 0u128);
    assert_eq!(p.cumulative_deposits(), 19u128);
    assert_eq!(p.pending_rewards(&s1), 0);
    sui::balance::destroy_for_testing(reward);
    sui::test_scenario::return_shared(p);
    destroy(s0);
    destroy(s1);
    sc.end();
}

